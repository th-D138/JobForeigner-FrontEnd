import styles from "./recruitInfo.module.scss";

const RecruitInfo = () => {
	return (
		<div className={styles.container}>
			<h1>Recruit Info Page.</h1>
		</div>
	);
};

export default RecruitInfo;
